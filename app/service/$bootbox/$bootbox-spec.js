describe('$bootbox', function() {
    'use strict';
  beforeEach(module('sdsTodos'));

  it('should ...', inject(function($bootbox) {

	//expect($bootbox.doSomething()).toEqual('something');

  }));

});
